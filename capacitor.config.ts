import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'investoMania',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
